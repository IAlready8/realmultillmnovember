// -----------------------------------------------------------------
// --- ADD THESE MODELS TO THE BOTTOM OF YOUR 'prisma/schema.prisma' ---
// -----------------------------------------------------------------

// Defines the subscription tier
enum SubscriptionTier {
  FREE
  PRO
  ENTERPRISE
}

// Defines roles within a Team/Organization
enum TeamRole {
  OWNER
  ADMIN
  MEMBER
}

// Model for team/organization
model Team {
  id        String   @default(cuid())
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  members   TeamMember[]
}

// Join table for Users and Teams, holding their role
model TeamMember {
  id        String   @default(cuid())
  userId    String
  teamId    String
  role      TeamRole @default(MEMBER)
  createdAt DateTime @default(now())
  
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  team Team @relation(fields: [teamId], references: [id], onDelete: Cascade)

  @@unique([userId, teamId])
}

// Model for managing user subscriptions (e.g., via Stripe)
model Subscription {
  id                 String           @default(cuid())
  userId             String           @unique
  tier               SubscriptionTier @default(FREE)
  
  // Stripe-specific fields
  stripeCustomerId   String?          @unique
  stripeSubscriptionId String?        @unique
  stripePriceId        String?
  stripeCurrentPeriodEnd DateTime?
  
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// --- MODIFY YOUR EXISTING 'User' MODEL ---
// --- Add these lines INSIDE the User model ---

// model User {
//   ... your existing fields (id, name, email, etc) ...
  
  teamMemberships TeamMember[] // User's teams
  subscription    Subscription?  // User's subscription
  
// }